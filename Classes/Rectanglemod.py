class Rectangle:
    def __init__(self, width, length):
        self.width = width
        self.length = length

    def area(self):
        area_var = self.width * self.length
        return area_var